{
  "name": "nutriscore-extension",
  "version": "1.0.0",
  "private": true,
  "description": "NutriScore Chrome extension build config",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "dependencies": {
    "firebase": "^10.12.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  }
}